mixin filter-status(filterStatus)
  if filterStatus && filterStatus.length
    ul.nav.nav-pills.mb-3.gap-2
      each s in filterStatus
        - const isAll = s.value === 'all' || s.value === '';
        - const href = isAll ? '?' : `?status=${s.value}`;
        - const isActive = s.active || s.class === 'active';
        li.nav-item
          a.nav-link.d-flex.align-items-center.gap-2.px-3.py-2.rounded-pill.fw-semibold(
            href=href
            class=isActive ? 'active shadow-sm' : 'bg-body-tertiary text-body-secondary border'
            aria-current=isActive ? 'page' : false
          )
            span= s.label

            
